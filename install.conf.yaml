---
- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.zaliases: zaliases
    ~/.p10k.zsh: p10k.zsh
    ~/.vimrc: vimrc
    ~/.zfunctions: zfunctions
...
