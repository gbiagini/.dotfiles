---
- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.zaliases: zaliases
    ~/.p10k.zsh: p10k.zsh
    ~/.zfunctions: zfunctions
    ~/.config/nvim/init.lua: init.lua
    ~/.config/nvim/colors: colors
...
